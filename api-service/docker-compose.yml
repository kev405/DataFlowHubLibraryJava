services:
  postgres:
    image: postgres:16-alpine
    container_name: dataflow_pg_prod
    environment:
      POSTGRES_DB: dataflow
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: